const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../labels';

describe('core.actions.labels', () => {
  it('should do something');
});
