const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../navigate_to';

describe('core.actions.navigate_to', () => {
  it('should do something');
});
